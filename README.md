MrteGestion
===========

DEV:
Il faut installer un serveur ftp en local avec un dossier   
GestionLicence
	-data.txt
	-config.txt
	-version.txt

Les fichiers signés sont dans le .zip apràs un maven clean install.

sous linux un bug gère un fichier data.txt vide lors de l'enregistrement, attention tuer le processus au lieu de quitter pour ne pas supprimer les data.
